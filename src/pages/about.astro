---
import Base from "../layouts/Base.astro";
import ThemeToggle from "../components/ThemeToggle.astro";
import LangToggle from "../components/LangToggle.astro";
import BottomNav from "../components/BottomNav.astro";
import { siteMeta } from "../data/siteMeta";
import { useTranslations } from "../i18n/utils";

const lang = "zh";
const t = useTranslations(lang);
---

<Base title={t("nav.about")} description="关于 Freetrip" lang={lang}>
  <article class="content-page">
    <div class="page-top">
      <a href="/" class="back-link">{t("back")}</a>
      <div class="page-top-actions">
        <LangToggle />
        <ThemeToggle lang={lang} />
      </div>
    </div>

    <h1>{t("nav.about")}</h1>
    <p class="subtitle">一些关于我的事情</p>

    <p class="spine-tagline">
      {siteMeta.spineTaglineCN}
      <span class="spine-tagline-en">{siteMeta.spineTaglineEN}</span>
    </p>

    <div class="prose">
      <p>你好，我是 Freetrip。</p>
      <p>一个对设计、代码和创作有持续热情的人。我相信好的设计是克制的，好的代码是简洁的，好的内容是诚实的。</p>
      <p>这些年我在几个角色之间往返：做过 AI 训练、在量化研究里挖过因子、用 Flutter 写些小工具，也一直保持写字的习惯。与其说是一条线性的履历，不如说是同一种好奇心的不同出口。</p>
      <p>这个网站是我的数字花园，用来记录想法、展示作品、归档记忆。它采用极简的编辑风格设计，像一本翻开的书——安静，但每一页都有分量。</p>
      <p>如果你有任何想法或合作意向，欢迎通过邮件联系我。</p>
    </div>

    <BottomNav lang={lang} />
  </article>
</Base>

<style>
  .page-top-actions {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }
  .spine-tagline {
    font-family: "Noto Serif SC", serif;
    color: var(--accent);
    letter-spacing: 0.05em;
    font-size: 1.05rem;
    margin: 0.4rem 0 1.8rem;
    line-height: 1.7;
  }

  .spine-tagline-en {
    display: block;
    color: var(--muted);
    font-style: italic;
    font-size: 0.9rem;
    letter-spacing: 0.03em;
    margin-top: 0.2rem;
  }
</style>
