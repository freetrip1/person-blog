---
// i18n: en 文章动态路由，复用 zh blog/[...slug].astro 结构；getStaticPaths 只取 lang==='en' 文章。
import Base from "../../../layouts/Base.astro";
import ThemeToggle from "../../../components/ThemeToggle.astro";
import LangToggle from "../../../components/LangToggle.astro";
import BottomNav from "../../../components/BottomNav.astro";
import { useTranslations, localizePath } from "../../../i18n/utils";
import { getCollection, render } from "astro:content";

export async function getStaticPaths() {
  const posts = (await getCollection("blog")).filter(
    (post) => post.data.lang === "en"
  );
  return posts.map((post) => ({
    params: { slug: post.id },
    props: { post },
  }));
}

const lang = "en";
const t = useTranslations(lang);

const { post } = Astro.props;
const { Content } = await render(post);

// i18n: 双语互链 —— 用 translationKey 查 zh 版本，存在则 page-top 渲染互链。
let translationHref: string | null = null;
if (post.data.translationKey) {
  const other = (await getCollection("blog")).find(
    (p) =>
      p.data.translationKey === post.data.translationKey &&
      (p.data.lang ?? "zh") === "zh"
  );
  if (other) translationHref = localizePath(`/blog/${other.id}`, "zh");
}

const marginalia = post.data.marginalia ?? [];
---

<Base
  title={post.data.title}
  description={post.data.description}
  ogType="article"
  ogImage={`/og/blog/${post.id}.png`}
  lang={lang}
>
  <article class="content-page">
    <div class="page-top">
      <a href="/en/blog" class="back-link">{t("back.blog")}</a>
      <div class="page-top-actions">
        {translationHref && (
          <a href={translationHref} class="translation-link">{t("post.translation.zh")}</a>
        )}
        <LangToggle />
        <ThemeToggle lang={lang} />
      </div>
    </div>

    <h1>{post.data.title}</h1>
    <p class="subtitle">{post.data.date?.toLocaleDateString("en-US")} {post.data.tags?.map((tag: string) => `#${tag}`).join(" ")}</p>

    <div class="prose">
      <Content />
    </div>

    {marginalia.length > 0 && (
      <div class="marginalia-list">
        {marginalia.map((m) => (
          <aside class="marginalia" lang={m.lang === "en" ? "en" : "zh-CN"}>
            {m.text}
          </aside>
        ))}
      </div>
    )}

    <BottomNav lang={lang} />
  </article>
</Base>

<style>
  .page-top-actions {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }
  .translation-link {
    font-family: "Noto Serif SC", serif;
    color: var(--muted);
    font-size: 0.85rem;
    border-bottom: 1px solid transparent;
    transition: color 0.2s ease, border-color 0.2s ease;
  }
  .translation-link:hover {
    color: var(--accent);
    border-bottom-color: var(--accent);
  }
</style>
